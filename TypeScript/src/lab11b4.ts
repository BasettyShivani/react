//****4 */
// • interface is mainly used for objects.
// • type can be used for objects, unions, and more.
// • interface supports extends (inheritance).
// • type supports intersections (&).
// • In practice, both work the same because TypeScript checks structure.
